import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  conatiner: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
    gap: 20,
  },

  title: {
    fontSize: 35,
    textAlign: "center",
  }
});