import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  image: {
    justifyContent: "center",
    alignItems: "center",
    width: 180,
    height: 180,
    marginBottom: 50,
  },
});
